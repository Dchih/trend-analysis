[project]
name = "product-radar-collector"
version = "0.1.0"
description = "Plugin-based collector runtime for ProductRadar"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
cache_dir = ".pytest_cache"
